using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IntelligenceTester : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        float JadenVariable = 1f;
        JadenVariable += 1f;
        Debug.Log(JadenVariable);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
